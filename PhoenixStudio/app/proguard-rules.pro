# Phoenix Studio — release ProGuard/R8 rules.
# isMinifyEnabled is currently false for both build types (see app/build.gradle.kts),
# so this file has no active rules yet. It is kept in place so enabling
# minification later is a one-line change rather than a new file.
